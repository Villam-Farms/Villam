# Database migrations

Run migrations in filename order in the Supabase SQL editor for the environment
being deployed. Apply `20260812_add_profile_onboarding.sql` before deploying an
API/frontend version that uses unified onboarding.
